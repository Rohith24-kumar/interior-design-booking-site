# MongoDB Atlas connection string (Atlas -> Connect -> Drivers). Never commit the real one.
MONGODB_URI=mongodb+srv://<db_user>:<db_password>@<cluster>.mongodb.net/nordic_designs?retryWrites=true&w=majority
